@using EFModel;
@using Common.Helper;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.Content.cshtml";
   
}
@{
    SYS_CONFIG infoModel = (SYS_CONFIG)CacheHelper.GetCache("SysInfo");
}

<div class="row">
    <div class="col-xs-12">
        <div class="space-6"></div>
        <div class="alert alert-block alert-success">
            <button type="button" class="close" data-dismiss="alert">
                <i class="icon-remove"></i>
            </button>

            <i class="icon-ok green"></i>

            欢迎使用
                            <strong class="green">@infoModel.SystemName
                                <small>@infoModel.SystemVersion</small>
                            </strong>
            ,©2017-2018 RandyField .
        </div>

        <div class="vspace-sm"></div>
        <!-- /span -->

        <!-- /row -->
        <div class="hr hr32 hr-dotted"></div>


        <div class="hr hr32 hr-dotted"></div>


    </div>
</div>



<script type="text/javascript">
    function initIframeHeight() {
        var userAgent = navigator.userAgent;
        var iframe = parent.document.getElementById("rightMain");
        var subdoc = iframe.contentDocument || iframe.contentWindow.document;
        var subbody = subdoc.body;
        var realHeight;
        //谷歌浏览器特殊处理
        if (userAgent.indexOf("Chrome") > -1) {
            realHeight = subdoc.documentElement.scrollHeight;
        }
        else {
            realHeight = subbody.scrollHeight;
        }
        $(iframe).height(realHeight);
    }

    jQuery(function ($) {
        initIframeHeight();   
    })
</script>
